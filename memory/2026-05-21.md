---
date: 2026-05-21
tags: [日记, 每日]
---

# 大闸蟹每日日记 - 2026-05-21

## 今日最重要的一件事

**飞书信息流 → Obsidian 收集系统跑通**：经过多轮 debug，最终拉取到 682 条消息，Collect Layer 搭建完成。

## 主要工作内容

### 🔧 飞书信息流项目
- **目标**：Karpathy 模式——所有飞书消息 → 原子笔记 → Wiki 知识图谱
- **架构**：Collect（蟹，每4小时）→ Review（Hermes，每日21:00）→ Wiki
- **发现 15 个可访问群**，确定全量拉取方案
- **踩坑修复 3 个 bug：**
  1. Bot 拉不到历史消息 → 改用 `--as user` 身份
  2. Python subprocess 环境变量缺失 → 加 `HOME` + `LARK_CLI_CONFIG_DIR`
  3. API 返回字段误读 → `data.messages` 而非 `data.items`
- **结果**：682 条新消息成功收集 ✅

### 📈 凤凰归巢盘后复盘
- 退潮深化，涨停从 61 家骤降至 34 家
- **0 可执行**；生物制品板块首次出现板块效应

### 📁 飞书测试消息
- 尝试用 bot 给 Johnson 发测试消息 → 失败
- 原因：`open_id cross app` — 不同应用生成的 open_id 无法跨应用发送
- 解决方案：需用 bot 自己的 open_id 或 Johnson 在当前 bot 下的 user_id

### 💬 模型切换讨论
- Johnson 问模型情况 → 确认当前 MiniMax-M2.7-Lightning
- Claudian Obsidian 配置调试（手动操作未完成）

## 今日教训
- `lark-cli` 在 Python subprocess 里必须有 `HOME` 和 `LARK_CLI_CONFIG_DIR` 环境变量才能读到配置
- Bot 的 open_id 和 user 的 open_id 是绑定 app 的，跨 app 无法互发

## 明日关注
- 飞书信息流 Review Layer 搭建（Hermes 每日 21:00 读 feed → 原子笔记）
- 5/30 嘉年华产品路演 PPT 准备

## 今日金句
> 682 条消息背后是 3 个 bug。每一个 0 都是通往正确路径的路标，不是墙。
